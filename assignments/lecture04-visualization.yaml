# 講義メタデータ
lectureNumber: 4
slug: lecture04-visualization
title: 第4回：データ可視化

# 課題リスト
assignments:
  - id: pandas-basic
    title: Pandasでデータフレーム操作
    packages: ["pandas", "numpy"]
    tests:
      - preCode: |
          import pandas as pd
          import numpy as np
          data = {'A': [1, 2, 3], 'B': [4, 5, 6]}
          df = pd.DataFrame(data)
        postCode: "print(result)"
        expected: "2.5"

  - id: pandas-histogram
    title: Pandasでヒストグラム統計
    packages: ["pandas"]
    tests:
      - preCode: |
          import pandas as pd
          data = pd.Series([1, 2, 2, 3, 3, 3, 4, 4, 5])
        postCode: |
          print(result.idxmax())
        expected: "3"
